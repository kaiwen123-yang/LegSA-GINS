# Experiment Protocol

Planned comparisons:

- Pure INS;
- single-antenna GNSS/INS;
- final_v23-style strong baseline;
- LegSA-GINS full method.

Planned ablations:

- without raw Doppler;
- without Go2 state augmentation;
- without source-aware weighting;
- without no-feedback smoothing;
- filter only;
- smoother only diagnostic.

Planned datasets:

- BY first dataset;
- indoor-outdoor transition;
- degraded GNSS;
- future high-quality RTK/raw GNSS dataset for separate future work.
