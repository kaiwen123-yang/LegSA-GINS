# T5bc-R bounded continuation authorization

Authorization: current human task **T5bc-R**. Starting HEAD: `d580c892` or its descendant. The original T5bc contract, D1–D8, D8 bounds and D12 classification policy remain frozen. Protocol v2.1 remains the sole manuscript chain; these outputs are sensitivity, limitations and v3 decision evidence.

## Preserved stop and exact repair

The original controller stopped at `BY2__F04__B3__D57_seed_00`: `legsa_v23_port_core_demo failed: ` precedes the otherwise exact formal module-activation error. All 3321 sidecar rows were invalid, the complete propagation loop contained 56642 iterations, and position/velocity/heading attempts were 1365/1371/0. The old terminal remains HARD_STOP. Its absent D6 effective-options echo remains `UNAVAILABLE_NOT_EMITTED`, never PASS.

The classifier accepts only the bare reason or the single fixed C++ demo prefix plus that reason. Full-window, expected/actual counters, enabled-heading input, all-invalid sidecar and zero-attempt conditions remain required. Other prefixes, suffixes or additional errors remain unclassified and hard-stop. A positively established B3 no-heading-epochs condition is reported as `B3_NOT_APPLICABLE_NO_HEADING_EPOCHS`, excluded from algorithm-failure counts and numerical distributions, and counted separately.

The candidate binary, frozen scalar binary, provider implementation and payloads, calibration, runtime-config clone rules, evaluator, windows, evaluation points, gating, covariance and process noise are unchanged. No rebuild, provider regeneration, identity native rerun, identity evaluator or parameter search is authorized. The only execution-code change is this controller repair and its explicit append-only continuation interface. Aggregate plumbing admits both registered freezes and adds the requested NOT_APPLICABLE accounting; it changes no evaluator metric.

## Append-only evidence and budgets

Original code freeze: `b0a9230f0711c7b81af9d868a98d0eb537ad6eb9`.

`<T5BC_SCRATCH>/09_HANDOFF/CONTINUATION_R/OLD_EVIDENCE_REGISTRY.json` SHA256:
`d1430d458c0cc7e77ef1edcef3d871d001c5b1cb796c81f45989914be838d6ec`.

The registry binds 62 old native summaries, 118 old evaluator terminal records (116 actual calls and two D27 skips), original launch ledgers, identity PASS receipts, original controller hard stop, partial-v1 records and member seals. Original D57 summary SHA256: `97c82826fc3e6752c53297815f7a95639e3a797fc1d76463f12d959f1619e46e`.

- Existing native calls: **62** (two identity, 60 matrix); existing evaluator calls: **116**.
- Remaining native: **199**, exact set difference from the complete 261-slot registration.
- Additional evaluator calls: **at most 398**, only v3/v2 for those 199 new slots.
- Total ceiling: **261 native / 518 evaluators**; D27 and D57 receive no new evaluation. The actually reachable evaluator ceiling is 514 after the two unavailable old natives.
- Identity admission uses the two existing byte/211-field PASS records and pinned candidate identity; no identity process is launched.
- Old native/evaluator directories, reservations, hard stops and partial ZIP remain unchanged. New reservations and control records live under `09_HANDOFF/CONTINUATION_R`; no old ledger is appended or rewritten.
- Each new terminal records the new code freeze; old terminals retain the original freeze. A report-only D57 adjudication references its original summary and never changes it.
- Native and controller trace reads: **0**. Each invoked evaluator subprocess opens trace exactly once. No retry of any reserved slot.

## Freeze, execution and deliverables

First commit message: `prereg(t5bc): continuation — classifier prefix fix, B3 not-applicable class`. Its full SHA, verified equal to the pushed `stage/clean3-math-repair` branch, becomes the continuation `code_freeze`; it is recorded in the execution receipt rather than creating a self-referential commit hash here. Only after this push may the remaining matrix start. A second results commit records terminal facts, tables, figures and verified v2 handoff identity.

The unchanged 61-case registry contains C00 and D01–D60 seed_00. Finish every remaining three-sequence candidate and all four subset candidates. Aggregate PILOT_TABLE v3/v2, LC01_CROSSCHECK, SUBSET61_TABLE/SUMMARY, paired distributions, BY2O segments, GATING_COUNTS, NIS_CONSISTENCY, attitude and the original requested figures. B3 NIS rejection rates are reported by sequence and condition; no-attempt denominators remain unavailable, not zero rates.

Read-only supplementary audit: compare D30–D41 B3 sidecars with the registered raw-source baseline, identify whether original dual_yaw injection reaches the sidecar, and explain any inherited HV-file differences from frozen provider lineage. Do not restore or redesign injection. Put D13/D14/D15/D27 B3 alongside the exact v2.1 F04 rows, with actual dual_yaw accept/attempt counts. These added readouts use sealed outputs and existing frozen tables only.

Full v2 handoff: `<HANDOFF_ROOT>/t5bc_v3_candidate_pilot_handoff_v2.zip`, with member SHA256/CRC/size verification and whole-ZIP SHA256. Preserve `t5bc_v3_candidate_pilot_partial_v1_handoff.zip` and the original hard-stop report.

## Stop and continuation policy

Hard-stop on candidate/frozen binary, evaluator, provider or frozen-table identity mismatch, config/identity gate failure, evaluator-process failure, trace-access violation, or a newly unclassifiable native failure. D8 divergence and positively classified D12/native applicability outcomes are recorded and execution continues. Preserve the original single-batch archive-I/O >1% hard stop. Bookkeeping conflicts are recorded in the results appendix; no scientific definition is adjusted.

## Validation

Synthetic regression covers the real fixed-prefix boundary, arbitrary prefix/suffix and extra-error rejection, incomplete-loop/nonzero-heading/count-mismatch rejection, preserved hard-stop seals, old-controller refusal, duplicate-reservation refusal, unavailable echo, exact 199-slot continuation and at-most-398 evaluator launch accounting. NOT_APPLICABLE is tested separately from algorithm failures. These tests perform no real native or evaluator calls and do not constitute scientific completion. Exact test totals and logs are recorded in the freeze/result receipts.
