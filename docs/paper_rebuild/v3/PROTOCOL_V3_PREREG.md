# V3-01 — 协议 v3 预注册

起点 `6257a4fa46efd11a8b8888c5e466cf923a45336e`；第 0 步提交
`d9df625cdbe80f3a767018c0a36f8974f94451a0`。本文件、合约、执行及汇总代码的
`prereg(v3): heading input 5 Hz raw stream — single change` 提交必须 push 后才成为
`code_freeze`。冻结值由实际 Git 回执记录，不能用草稿 HEAD 代替。

## 唯一改动与不变项

唯一科学改动：双天线标量航向从 status 航向输入改为原始 HPPOSECEF1/2 精确配对的
5 Hz 航向。使用不改动的 T5a-R `raw_yaw_from_ned` / `build_t5a_variants`，沿用
GNSS2−GNSS1、冻结坐标变换、安装关系及 wrap-safe 标量残差。

冻结二进制 SHA256 为
`96ae436d82ba8922c68382bd73fc42c8bf4bcb22d72a43a8bd05f506043a9c1c`；
冻结评估器 SHA256 为
`aa0492482b6467cdd701bc8882aca11c4170bbe2e7c6bb5f932679dc204978da`。
不修改 C++、任何运行参数、IMU、位置、速度、RD、RP、HV、工况、种子或配置集。
GNSS18 只允许第 14 列 yaw 和第 18 列 yaw_valid 改变；其余 token、空白、行序及时间字节不变。

三序列基准 std 标记仍为 `2.933193`。它是继承的 v2.1 固定标记，本任务不声称
重新标定了 5 Hz 航向噪声或独立性。各注入工况的 std 列逐行保留，包括
D36/D37/D38/D60 原有缩放；旧 1 Hz 航向行与中间行可能有不同 std。
F01 沿用 v2.1 实际复用的冻结 F01 配置和 provider；其不启用航向更新的原 std 字节也保留，
不为统一标签另改输入。不同原 GNSS 哈希分别生成衍生表，不把 F04 provider 替换给 F01。

所有方法及注入工况的 HV 文件均按原哈希复用。其旋转继续使用 status 航向及原注入后的
有效性处理，不用新 5 Hz 航向重建。故 v3 的直接航向输入与 HV 旋转的航向来源、采样和
故障暴露不完全一致；不能宣称整个航向依赖链均已变成 5 Hz。此边界与 T5a-R/T5bc 一致。

## 0a：已核验事实与人工裁定

第 0 步原硬停记录保留。用户随后明确回复“按核验事实修正 0a 并继续”，授权采用以下事实；
不改写先前记录为从未阻塞。

| F04 三序列 | yaw RMSE 之和（deg） | 相对 R5 的下降（deg） |
| --- | ---: | ---: |
| R5 | 6.2538569227651107 | 0 |
| R5σ | 6.1442162463454558 | 0.1096406764196549 |
| R5W | 6.1050993524129546 | 0.1487575703521561 |

两项改善均未达原 0.3° 门槛。61 例中冻结 F04 有限 **60**、R5 有限 **58**、配对有限 **57**。
四例是两侧失败集合并集，不是冻结侧的四个非有限工况。

| Case | 冻结 F04 | 冻结 yaw RMSE（deg） | R5 |
| --- | --- | ---: | --- |
| D27_seed_00 | COMPLETED | 32.472978585312966 | ALGORITHM_FAILURE_DIVERGED |
| D37_seed_00 | ALGORITHM_FAILURE_ALL_YAW_REJECTED | UNAVAILABLE | COMPLETED；1.8862718548526467° |
| D57_seed_00 | COMPLETED | 1.7870833845921381 | ALGORITHM_FAILURE_NO_VALID_HEADING_INPUT |
| D60_seed_00 | COMPLETED | 1.9413335363795345 | ALGORITHM_FAILURE_DIVERGED |

因此明确记录 R5 在冻结完成的 D27/D57/D60 上失败，不写“无新增失败”。
两版原逐例数值及来源哈希见第 0 步 CSV/JSON，均经过独立只读复核。

## 0b：有效性选择

复用 T5a-R 已封存 error_series，使用 H-EXT-04L 同一分段函数。
主段闭区间 `[3369.94,3411.95]` s，次段 `[3495.94,3508.94]` s；全窗 `[3186,3563]` s。

| 配置 | 变体 | 主段 yaw RMSE（deg） | 次段 yaw RMSE（deg） |
| --- | --- | ---: | ---: |
| F02 | R5 | 0.8630369665699419 | 1.5792117145356600 |
| F02 | R5F | 0.8862759689859719 | 1.7608525803877044 |
| F04 | R5 | 0.2325140820805299 | 1.8615421941989120 |
| F04 | R5F | 1.0351701650068426 | 1.1297378551589390 |

规则原样：若 F04-R5F 主段 yaw ≤ F04-R5 主段 yaw，则两接收机均为载波相位解
（各自 fixed 或 float）；否则两台均 fixed。实际 `1.0351701650068426 > 0.2325140820805299`，
选定 **R5：两台接收机均 fixed（NAV-PVT carrSoln 都为 2）**。
v3 为主评估器，v2 平行；以上 yaw 数值两版一致。0b 未重新启动 native/evaluator 或打开 trace。

## 航向故障在 5 Hz 表上的时间语义

故障参数、9 个种子及已选 anchor 不变。具体逐工况窗口完整列在
`HEADING_FAULT_WINDOWS.md`，来自本次哈希冻结的原 provider bundle。
自然序列使用未注入的原始流；Canonical 和 A1/A2 保留半合成标签，分别汇总。

原冻结航向源在 BY2 有 301 个有效时刻 `56,57,…,356` s。
对已实现的随机角度扰动，先取原 case 输入航向减原基准输入航向的圆周差，
再在 `[t_i,t_{i+1})` 时间单元内加到新原始航向；最后一个单元为 `[356,357)`。
`[56,357)` 外保留新原始航向，不延长末次扰动。不读取任何 solver 输出进行此映射，
不重新抽取噪声、尖峰或事件。它是冻结扰动的时间单元延拓，不是独立 5 Hz 高斯采样。
标量差只用于已登记的故障注入，不是估计输出校正。

| 族 | 5 Hz 映射；所有非航向列及文件保持冻结字节 |
| --- | --- |
| D30 | 原 anchor 中心 5 s 半开停测窗，窗内 yaw_valid=0；窗外按 R5 有效性。 |
| D31 | 原 anchor 中心 20 s 半开停测窗，同上。 |
| D32 | 原 σ=1° 的已实现有符号扰动，按原 1 s 单元延拓。 |
| D33 | 原 σ=5° 的已实现有符号扰动，按原 1 s 单元延拓。 |
| D34 | 原 5% 已选 ±10° 尖峰单元，事件位置和符号不重抽。 |
| D35 | 原 10% 已选 ±10° 尖峰单元，事件位置和符号不重抽。 |
| D36 | 原 std×1.5 的逐行 token 不变；新航向中间行仍保留原未缩放 std。 |
| D37 | 原 std×3 的逐行 token 不变；不预先复制冻结算法失败标签。 |
| D38 | 原 σ=10° 的有符号扰动按单元延拓；原 std×0.25 仅保留原有行。 |
| D39 | 每 seed 的三个冻结秒制停测区间直接用于 5 Hz 表；不重新按“2–4 行”选突发。 |
| D40 | baseline-length/rel-acc 元数据扰动继续无标量运行通路；不引入 3D 测量。 |
| D41 | 仅转移原单接收机不对称故障已产生的标量圆周角差；不重建或评估 3D baseline。 |
| D57 | 冻结各源 latency/jitter 后的时间 token 保持原样；只精确匹配原始 iTOW，不插值、不取邻行、不修正时间。有效航向行为 0；按 v2.1/D12 分类规则区分无有效输入、全拒绝、发散与技术失败。 |
| D58 | 原 10 s 位置停测窗内的 ±10° 已选尖峰单元；与原故障窗取交集；原 20 s 恢复窗保留。 |
| D59 | 航向未注入，使用 R5 原始航向；坏位置及 RD 冲突保持冻结文件。 |
| D60 | 原 20 s 复合故障窗内的已实现 σ=10° 航向扰动单元；逐行 std 原样；原 20 s 恢复窗不变。 |
| D61 / A1 | 原 10/20/30 s 全 GNSS 停测窗内 yaw_valid=0；位置、接收机速度、RD 及各 case HV 文件不变。 |
| D62 / A2 | 原 10/20 s 位置及全部速度停测；新 R5 航向按原始有效性保留，HV 原样。 |

其他含航向作用的混合族亦闭合：D06 沿用原 20 s 航向停测窗；D08/D09/D10 沿用
原确定性相位分箱算法，目标 5/2/1 Hz、相位 `anchor mod (1/rate)`、每箱保留首个有效行；
D11/D12 将原已选 1 Hz 掉线时刻延拓为相同 `[t,t+1)` 单元，不重抽 30%/60% 事件。
其余族仅换原始航向，所有原非航向注入保持。std 故障仍主要暴露于原 1 Hz 行、
随机故障是时间相关延拓、HV 保留 status 来源，三项限制必须随表和手稿报告。

## 身份门与顺序

| 门 | 必须满足 |
| --- | --- |
| 2a | 三序列新 GNSS18 及所有 case 衍生表逐列差异报告；除 yaw/valid 外全字节相同。 |
| 2b | BY2 C00 主表 SHA256 等于 T5a-R R5：`fc6dd49c0a35c41208fc7d94da1c78ad63fe6938bd8f599021750c3bf5e20760`；三序列相应哈希亦逐项检查。 |
| 2c | 全部配置逐行文本克隆，只替换 gnsspath 一行；其他字节一致。启动前核对全部 211 项期望回显覆盖；每个运行输出实际 211 项回显后精确比较，才能接纳结果。 |
| 2d | 新 F01 三序列完整 NAV 与冻结 v2.1 实际 F01 完整 NAV 的封存字节哈希一致。 |
| 2e | 新 F04 BY2 C00 完整 NAV 等于对应 T5a-R R5 完整 NAV；目标 SHA256 `2fda678fbc9d760e7bd4b93b6e7f99861b41ea91aef86252566ea059abbeb0f4`。 |

2c 的实际运行回显不能在 native 启动前宣称已得到。历史失败未发出回显时，只可使用
科学配置字节相同、差异限于身份/路径的冻结见证生成**期望值**，不能伪造历史回显。
本次失败未发出回显仍记 `UNAVAILABLE_NOT_EMITTED`，不能填 PASS；失败类别须有独立
完整证据，无法分类即硬停。任何已发出回显的差异均立即硬停。

顺序：push code_freeze → fresh providers/2a/2b → 所有配置文本门与期望覆盖 →
三次 F01 和一次 BY2 F04 身份运行 → 完整矩阵。四个身份运行占用正式队列并直接复用，
不得重复调用。完整 NAV 身份不可用稀疏 NAV_10HZ 代替。

## 全量登记、执行与报告

11 个唯一配置：F01、F02、F03、F04、A03、A04、A05、A06、A07、A08、A09。
F03/A02 与 F04/A01 为逻辑别名，不重复运行。
5951 个 Canonical-541 运行 + BY2H/BY2O 22 个 C00 运行 + A1/A2 495 个运行，
总计 **6468 个唯一 native**、最多 **12936 个 v3/v2 evaluator**。
旧配置审计的 6539 数量包含额外审计与 ladder/grid，本任务不运行这些额外项目。

native 64 路并行，各数值库一线程。每次 native/evaluator 启动前 fsync 唯一预约，
失败消耗名额，不重试。每 64 项分批留存；新生成 scratch 大文件仅在无损归档、原字节
SHA256 校验完成后按逐项清单释放，保留完整恢复信息与证据；不得触及任何冻结文件或根。
trace 仅由冻结评估器子进程读取一次；provider/controller/native 不读 trace。
绘图所需 Truth 只从同一评估器进程已读取的引用数组观测导出，不新增 trace 读取，
不改变评估器或计算结果。

分类沿用 D12：可确定的算法失败记录后继续；任何身份门失败、非航向差异、二进制或
评估器哈希不等、分类不了的失败立即硬停，不自动改代码续跑。停止时保留已消费预约及全部证据。
并行硬停立即停止新派发并取消尚未开始的任务；已启动子进程允许收束并封存，
不再启动相关评估或下一批。硬停报告列出检测时及收束后的实际消费数量，不把在途运行隐去。

决策规则：**不设数值通过门槛，全量报告 v3 结果和失败**；不得按指标删工况、删历元或择优汇报。
输出两版评估、52 行三序列主表（只替换 LegSA 行，LC01/EXT05C 行直接保留原来源）、
F01→F02→F03→A04→F04 消融、541 汇总分布/失败对照、61 例配对、A1/A2、
BY2O 主段/次段/段外块。T5bc-R R5σ/R5W/B3 三序列及 61 例尾部作为原来源敏感性块引用。
重出 MFIG00–06、SFIG01、FIG02S、FIG02S-b；v2.1 原图保留。
提供受影响手稿方法段落/表格替换文本，以及完整交接 ZIP、成员校验和全包 SHA256。

时偏、B3、R5σ/R5W 不执行：时偏及模型拓展写入限制与未来工作；T5bc-R 既有权重/3D
证据进入敏感性或补充材料，不作为此次新配置，也不选择胜者替代预注册 v3。

## V3-01-R 存储与中断恢复追加（2026-09-19）

原科学冻结 `7d43b9af26120ed5dde21f53e515386361072ba6`、C2 的 R5 双 fixed
规则及全部科学内容不变。本追加覆盖上文 64 路分批和全量无损归档的存储条款。
采用 22 路、每批 22 项；结果仅在 `<CLEAN_ROOT>/stages/CLEAN8_PROTOCOL_V3`，
打包仅在 `<HANDOFF_ROOT>`，唯一 WSL 中转为 `<PROTOCOL_V3_SCRATCH>`，上限
20 GB。G: exFAT 写入采用已有重试包装和复制后逐文件 SHA256，不依赖原子替换。
每批记录 NAV/STD、评估输入哈希、双版评估与 RUN_MANIFEST，G: 校验通过后按精确
清单删除 scratch；完整 NAV/STD 不保留。任务结束 WSL 无 ARCHIVE 或逐例输出。

每批前检查 E: 真实可用不少于 40 GB、G: 不少于 30 GB、scratch 不超过 20 GB；
失败记录暂停十分钟，连续三次不满足硬停。禁止使用 `df /` 虚拟容量替代 E:。
事故属于存储中断；原批账本、预约、科学身份门和现场记录保留。B2 已核对通过的批
直接复用；仅完整登记的中断/未通过批整批重跑，已有 NAV/STD 哈希必须重现。
四次已通过身份运行不再启动 native。新的恢复预约写独立账本，原预约不删除或改写。

独立 `tmux v3` 控制器把日志、批账本、每 60 秒心跳、STATE 与 PROGRESS 写入
`00_CONTROL`。主进度显示核心非 F01 的 5,410/10,820，另列核心 F01、三序列额外
行、A1/A2 与全部 6,468/12,936；队列未改变。P4 未验证先验证，已验证只续删隔离区；
矩阵从最后一个校验通过批继续。完成后自动汇总、出图、机器 QA 并写 DONE/SUMMARY；
视觉校对、手稿替换、完整交接包和结果提交留待后续会话。

本追加 GB 均指 1,000,000,000 bytes；进度另报精确字节。
逐文件删除意图与完成检查点写入每个归档槽的追加式账本，并逐行 fsync；不在
exFAT 上为每个检查点创建单独小文件。两条已核验的额外身份 native 提前计入总进度，
其正式行仍在原队列位置入表，既不重复预约也不重复执行。

## MATRIX 前容量预测与误差序列保留追加（2026-09-19）

以已核对通过的前 8 批（512 native、1,024 evaluator）在 G: 的实际目录为样本，
分别执行 `du -s -B1` 与 `du -s -B1 --apparent-size`。按剩余 5,956 个 native
归档槽与 11,912 个 evaluator 槽外推；其中两条额外身份 native 已执行，实际新增
native 调用为 5,954。聚合、十图和控制记录另预留 20,000,000,000 bytes；这是
工程规划余量，不是实测科学结果或峰值，最终 handoff ZIP 另计。

测量时 G: 可用 186,261,962,752 bytes。保持全部误差序列时，MATRIX 加上述余量
预计新增分配 166,798,532,608 bytes，超过可用容量 60%（111,757,177,651.2 bytes），
因此触发用户授权的精简保留规则。预测依据和逐槽双口径实测保存在
`<V3_ROOT>/00_CONTROL/CAPACITY_FORECAST.json` 与 `CAPACITY_SAMPLE_8_BATCHES.csv`。
预测会随进度持续显示，不替代每批实际容量检查。

精确保留集合为 CORE 的 `C00_clean_normal` 及 `D01_seed_00` 至 `D60_seed_00`，
另加全部 SEQUENCE 和 ADDENDUM 域；全部 11 配置、v3/v2 两个版本一并保留。
即 1,188 个 native 对应的 2,376 个评估槽保留误差序列；其余 5,280 个 CORE
native 对应的 10,560 个评估槽仅不保留误差序列 payload。所有槽的评估结果、
RUN_MANIFEST、原 OUTPUT_SEAL、输入/输出哈希与归档回执照常保留。
算法失败原本没有误差序列时仍按原类别记缺失，不补造序列。

MFIG00 的 C00 五方法、MFIG05 的 D04/D12/D27/D58/D60 seed_00
（F03/A04/F04）均包含在上述集合；另保留 RUN_00004/v3 的 MATCHED_TRAJECTORY
及 manifest，供 MFIG00 Truth 使用。BY2O 全部方法双版本全窗序列完整保留，
不裁去主段、次段或段外。冻结十图的其余输入来自聚合表和记录。

已归档 512 例不重跑、原核对记录及其 pins 不改写；完成 B2 后以独立保留回执和
精确删除账本处理未入集合的误差文件，删除前核对存储字节哈希与解压内容哈希。
新批归档绑定相同保留清单。预计后续新增分配约 109,706,047,488 bytes（含上述
20 GB 余量）；已归档样本另可释放约 5,190,189,056 分配字节。这些均为容量预测，
不是删除完成量；实际删除回执和每批容量记录分别报告。

逐运行保留清单为本目录 `ERROR_SERIES_RETENTION_INDEX.csv`（6,468 行，各行适用于
v3/v2），SHA256 `c94ab6244a3ffa7cd4064a9f48995ce7f4918aa1687605bd5e04bc890a5a767a`。
G: `RETENTION_POLICY.json` SHA256 为
`cce99125efced77a08ebe4eddb54599dfb87cec55aab96a7a5ce7bb522535e3d`，绑定容量预测
SHA256 `6f7286f3c0db7206c4b3c19c165d68fc4e7b79779498619b1111d5acc59c925e`
及原冻结 REGISTRY。生成此清单仅写存储元数据，未删除文件、未调用 native/evaluator。
