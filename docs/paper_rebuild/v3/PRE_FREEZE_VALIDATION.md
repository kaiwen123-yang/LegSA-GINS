# V3-01 pre-freeze validation

Status: READY_FOR_PREREGISTRATION; actual scientific gates 2a–2e NOT_RUN.

The user explicitly accepted the verified 0a facts and authorized continuation.
The first prerequisite commit remains immutable, including its historical stop.
The corrected facts and selection rule are recorded in PROTOCOL_V3_PREREG.md.

Read-only registry recovery verified 6468 unique runs: 5951 core, 22 additional
sequence C00, and 495 addendum. Exact case identities, nine frozen seed values,
11 configurations, 30 source ledger hashes, and 177 historical missing-echo
expectation witnesses passed. The witnesses do not replace actual emitted echoes.

Provider provenance review checked 585 frozen bundles and 585 seals, three base
tables, and sequence contracts. No provider generation, native execution, or
evaluator invocation occurred during this readiness check.

Final focused validation: 33 tests passed (7 provider, 7 runtime, 19 reporting).
The reporting suite exercised all ten requested figure drawers in memory with
synthetic fixtures, including a wholly unavailable degradation type; these
fixtures are tests only and are not real-data evidence or exported result tables.
Earlier compatibility regression: 96 passed. Matplotlib/Pyparsing emitted existing
environment warnings; the required two-dimensional drawers and checks passed.

Independent final review passed after checking frozen component windows, fixed
heatmap type positions, CLI imports, exact registry identities, source pins,
hard-stop cancellation/drain records, and manuscript limitations. Scope and path
checks found no C++ changes, no frozen source changes, no local path leaks, and
no runtime payload staging. Unrelated pre-existing drafts remain untouched.

This receipt is code readiness only. Execution starts only after the preregistered
commit is pushed and independently verified as code_freeze. Identity gates and
all scientific completion claims require fresh runtime evidence.
