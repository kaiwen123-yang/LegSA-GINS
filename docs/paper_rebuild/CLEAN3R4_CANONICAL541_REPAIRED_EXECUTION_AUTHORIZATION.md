# CLEAN3R4 repaired Canonical-541 execution authorization

The human authorization `CLEAN3_REPAIRED_CANONICAL541_FAST_RESTART_AND_AUTOMATIC_COMPLETION`
supersedes the earlier Canonical-541 preparation-only boundary for one repaired,
non-overwriting matrix attempt. The active stage identity is
`CLEAN3R4_BY2_CANONICAL_541_REPAIRED_MATRIX`; the old
`CLEAN2R2B_BY2_CANONICAL_541_CASE_MATRIX` attempt remains read-only.

The old 3298+ method-bound manifests are not formal inputs under the repaired
solver freeze. Provider payloads and content-addressed shared GNSS bytes may be
reused only after their byte hashes, generator/config semantics, raw lineage,
effect-validation results, and offline-trace boundary close. All 5951
method-bound manifests must be rebuilt under the repaired code/executable and
attempt identity.

Execution is authorized only after one compact readiness gate: build and tests,
the read-only exact 541 x 11 registry preflight, AB0000 strong-anchor parity,
and one clean 18-configuration run. The resulting solver/config freeze is
immutable after the first matrix solver launch. The matrix must reach 5951
terminal unique identities and seal outputs/failure proofs with zero reference
trace reads before the exact offline evaluator can open the trace.

All data-role, no-tuning, no-output-correction, no-metric-rerun, and forbidden
legacy-input rules remain unchanged. This authorization does not permit a new
degradation design, literature comparison, merge, release tag, or deletion of
historical attempts.
