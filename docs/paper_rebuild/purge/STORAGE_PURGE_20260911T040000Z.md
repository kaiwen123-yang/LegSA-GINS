# P-09ab storage inventory and stopped quarantine — 20260911T040000Z

**Terminal: STOPPED_C4_FAIL_NO_FILES_MOVED_OR_PURGED.** C1–C3 passed. C4 failed when G: rejected `renameat2(RENAME_NOREPLACE)` with `EINVAL` (errno 22). C5 and D were not executed. Zero source files moved; zero files purged; zero bytes released by purge. The batch root and six empty descendant directories remain in quarantine. No retry, fallback move, or purge followed the observed C4 failure.

The requested start commit `68ee6b3c5e918f5b24f25e442f7bb13c5fecb263` was pushed first. The storage policy was committed and pushed before inventory. P-09c was not executed.

## Inventory and exact plan

Audit: `<CLEAN_ROOT>/storage_purge/20260911T040000Z/`.
Quarantine: `<CLEAN_ROOT>/_PURGE_PENDING/20260911T040000Z/`.
Policy: `configs/paper_rebuild/clean6/STORAGE_PURGE_POLICY.yaml`.

| Classification | Regular files | Logical bytes |
|---|---:|---:|
| KEEP | 340,905 | 106,342,003,058 |
| BULK_DELETABLE | 57,233 | 439,508,000,426 |
| UNKNOWN | 40,761 | 101,410,733,102 |
| Total | 438,899 | 647,260,736,586 |

There are 494,726 recorded entries, including 55,827 directories and no symlinks/special entries. `STORAGE_INVENTORY.csv` contains recursive totals for 19 stages and 14 attempts. Stage rows include their attempt descendants, so rows across levels must not be added. The original 33-row partitioned table is preserved byte-for-byte as `STORAGE_INVENTORY_PARTITIONED.csv`. All per-class file and byte totals close against `FILE_INVENTORY.csv`; plan/control ledger bytes remained unchanged during this metadata-only conversion.

The complete UNKNOWN list is `UNKNOWN_FILES.csv` in the audit root: 40,761 regular files / 101,410,733,102 logical bytes, all retained. `DELETION_PLAN.csv` lists all 57,233 candidates. Exact-path sealed SHA-256 evidence covers 45,136 candidates; 12,097 received fresh streaming SHA-256. `PLAN_SUMMARY.json` groups the plan by file class and family.

| Eligible family | Planned files | Planned logical bytes |
|---|---:|---:|
| canonical541_v1 | 35,238 | 310,687,436,893 |
| clean5_p07 | 21,342 | 124,884,330,322 |
| clean5_parity_ladder_and_p05 | 479 | 2,804,210,993 |
| clean5_sequences_v1_v2 | 158 | 944,277,044 |
| superseded_attempts | 16 | 187,745,174 |

KEEP precedes every bulk rule. The 1 MiB JSON/CSV threshold is a conservative superset of the requested 1 MB per-run protection. All direct handoff source files, providers, the entire CAL stage including its 15 runs, seven final CLEAN4 method roots and Git worktrees are retained. `EVAL_NAV.csv` remains KEEP/UNKNOWN because it is a direct solver export without evidence that it is an approved staging copy. There are zero `temporary_regular_file` candidates. No files were added to the plan because of space pressure.

## Gates and terminal evidence

| Gate | Result | Evidence |
|---|---|---|
| C1 | PASS | 57,233 candidates eligible; KEEP matches 0, outside stages 0, invalid class/family 0 |
| C2 | PASS | 1,660 reference checks; 846 required paths, 761 file exemptions, 13 retained subtrees, 11 source pins; unresolved 0 |
| C3 | PASS | Every candidate has path, size, SHA-256, evidence and planned quarantine timestamp in the immutable ledger |
| C4 | FAIL | `renameat2(RENAME_NOREPLACE)` returned EINVAL; one MOVE_PREPARED, zero MOVED_HASH_VERIFIED |
| C5 | NOT_EXECUTED | C4 failed; no post-quarantine record test was represented as PASS |
| D | NOT_EXECUTED | The explicit any-C-failure stop rule applied; no unlink or purge |

`GATE_PRE_MOVE.json` is the successful C1–C3 receipt. `GATE_QUARANTINE.json` is explicitly a supervisor-recorded failure from the service output and journal; the runtime created no successful C4 receipt. `PURGE_ABORTED.json`, `C4_FAILURE_REVIEW.json`, and retained service journals record the stopped state. `PURGE_RESULT.json` does not exist.

The exact failed source is:

```text
<CLEAN_ROOT>/stages/CLEAN3R4_BY2_CANONICAL_541_REPAIRED_MATRIX/
.attempt_20260808T192851P0800/01_COMPACT_READINESS_RUN/
CLEAN18_OUTPUTS/03_AB0000/KF_GINS_Navresult.nav
```

The independent reviewer rehashed this one source only: 10,025,634 bytes, SHA-256 `480cc9c272bbfdc30842bac6248612847e3d394a50b2b97ddaa1ea8c5631cdd2`, matching its plan row. It remains at its original path; the destination does not exist. The other 57,232 candidates were not attempted. The service has MainPID 0, state failed, exit code 2.

The journal contains exactly one valid `MOVE_PREPARED` event at `2026-09-11T05:37:31.420412+00:00`, SHA-256 `ce27eb7679bf1ed7abd43697d889e354b3c06250e76c055257238ecc81153be9`. There are no verified move, purge preparation or purge completion events. The quarantine has zero files, links or special entries; its seven directories (batch root plus six descendants) are retained.

An audit-owned rename capability probe also returned errno 22. The same composed tool sequence issued the quarantine launch before that probe's nonzero exit was inspected. The quarantine service then independently failed at its first rename. This dispatch error is retained here and in `PURGE_ABORTED.json`; the service journal directly proves the quarantine failure, while the probe/dispatch ordering comes from the supervisor's tool record. The probe source remains in the audit directory. No fallback or retry was issued after observing C4 failure.

## Checks and read-only execution history

The completed inventory used `a170e2f70be53b5d533cd96fd045261fc10f4465`: 16 metadata workers, eight seal readers and four hash workers. Of the KEEP entries, 153,142 used independently qualified native metadata with identity fields explicitly UNQUERIED. Every candidate retained full LINUX_LSTAT / QUERIED / nlink=1 identity. Independent replay found zero classification, KEEP/reference/subtree intersection, path or quarantine-map errors. CAL's 907 regular files and CLEAN4's 7,256 final-method regular files are all KEEP.

The first two read-only inventory sessions stopped before producing a plan or quarantine while addressing DrvFs metadata cost. An optimized service then lacked PowerShell in its PATH; the next encountered Windows legacy path-length handling. Both stopped without source movement. After correcting the service PATH and extended-length path handling, inventory completed with exit 0. On the precise long directory, native/Linux names matched 3,981/3,981, reparse count was zero, and six size/type samples matched. Seal indexing parses recorded paths lexically and intersects them with real candidates; actual seals and candidates retain filesystem safety checks.

Validation:

- Utility and successful-finalizer guard tests: 164 passed. The successful-finalizer was not invoked on this failed batch.
- Recursive inventory converter tests: 12 passed; actual conversion PASS, with all classification totals closed and plan/ledger hashes unchanged.
- Pre-quarantine real-record baseline: 20 passed, zero skipped (six CLEAN_ROOT record checks plus 14 Outcome scope checks). These are baseline results, not C5.
- Independent pre-quarantine record baseline: 331 CLEAN5 aggregate files, actual A04/F04 decision sources, CAL's 15 run records and both 15-row evaluation tables readable; frozen/current decision-rule bytes match outside the authorized Outcome block.
- Broader pre-quarantine repository baseline: 46 passed, two pre-existing failures in CODE_ROOT whole-document hash checks for BY2H/BY2O. Current AGENTS differs from its historical pin; the rule also has its authorized Outcome append. These tests were not changed or marked PASS. They do not read CLEAN_ROOT records.

No scientific execution, raw payload modification, parameter change, main-chain change or Outcome change occurred. The only post-failure work was source verification and audit/document archival. Since zero roots lost bulk, AGENTS §3 and handoff §6 record the failure without a `bulk purged` annotation.

## Capacity and literal ledger archive

- Planned potential release: 439,508,000,426 logical bytes.
- Actual files moved/purged: 0 / 0; actual purge release: 0 bytes.
- G: initially available: 56,603,181,056 bytes.
- G: available at stop (`2026-09-11T05:39:28.280537+00:00`): 56,298,307,584 bytes.
- Post-clearance available space: UNAVAILABLE — no clearance occurred. Capacity evidence is `FREE_SPACE_AFTER_STOP.json`; audit records consume space, so current free-space change is not a purge release.
- P-09c remains unexecuted; no post-purge capacity conclusion or execution-mode transition was made.

The tracked archive contains the **original PLANNED control ledger bytes**, not a completed execution ledger:

`docs/paper_rebuild/purge/DELETION_LEDGER_20260911T040000Z.json.gz`

- Decompressed ledger: 75,179,540 bytes; SHA-256 `fa191c3b24d797453a1a54adf5ecf10168ef5be47f8b7fa49fceb7811f08d971`.
- Gzip: 3,335,612 bytes, mtime 0; SHA-256 `798712fa59363d8c4e4c3165079aaf32755b90af0e510c9f185ce0476f2f514b`.
- Gzip decompression exactly equals the unchanged audit `DELETION_LEDGER.json`.
- Plan SHA-256: `6998bf22f009c95d8a4b0fb4c431b20b79a3bd15447d6239fe53c92cda84624b`.
- Recursive inventory SHA-256: `6209cb9df100f8f6baa31468351f0b2373b8362cae4d9d06ee1845d6176966a8`.

## Commits

| Commit | Record |
|---|---|
| `68ee6b3c5e918f5b24f25e442f7bb13c5fecb263` | Requested start, pushed first |
| `6613149f5afba7a6f2bbf083896506a739942003` | chore(storage): preregister P-09ab purge policy |
| `b81e7e058be9f6239317bba472e048ec300e5fa5` | feat(storage): exact-file purge guards and reference retention audit |
| `1adb8281424a07bf72278bb1342e998cc7a13139` | perf(storage): bound metadata concurrency and pin handoff sources |
| `2d79e20aaf207b64bcbed4f6938dc30a46a79af6` | perf(storage): optional native metadata for dense retained records |
| `6cb0803f862c43b3d3f40bb38cb3410937ca814c` | feat(storage): preserve control ledger and archive verified execution ledger |
| `8dc37d0701ab9565028e6cc71be4fadf2fd3f8a1` | perf(storage): index sealed paths without redundant filesystem traversal |
| `a170e2f70be53b5d533cd96fd045261fc10f4465` | fix(storage): support long Windows paths in retained metadata inventory |
| `00c3b2e9844336fa998b66d1a0298d211b671264` | fix(storage): report recursive stage and attempt inventory totals |
| this commit | Requested P-09ab record commit; C4 failure, no completed purge |
