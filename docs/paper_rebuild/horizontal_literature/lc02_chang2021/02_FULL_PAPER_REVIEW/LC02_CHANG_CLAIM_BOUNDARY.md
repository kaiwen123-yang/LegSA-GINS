# Paper claim boundary

The paper directly claims improved accuracy, stability, adaptability, and robustness in its own simulations and Barton flight-data experiment. Those claims were reviewed as paper context only. No paper RMSE value, ranking, trajectory, runtime output, or parameter was imported as current project performance evidence.

The current work establishes source identity, equation identity, unresolved semantics, input availability, and method distinctness only. It does not establish a faithful FSTCKF implementation, BY2 accuracy, mechanism activation, or comparison superiority.

The uploaded subscription PDF is legitimate source evidence but is not redistributed. No official Chang implementation was found. Cited algorithms are completion candidates only where the Chang paper explicitly relies on them; none can silently repair Eq. 12, the 15-state fading map, or the missing fuzzy aggregation.

The formal terminal is `NO_GO_LC02_CHANG2021_FSTCKF_AS_FORMAL_PRIMARY`. All implementation, solver, C00, representative-case, and comparison authorizations remain false.

Access disclosure: one broad tracked-active search accidentally rendered internal-report performance lines; those values were quarantined and not used. An initial GNSS1 name census materialized serialized nonselected `data` cells, but decoded and used no forbidden payload semantics. Subsequent payload decoding was PVT/NAV-COV selective. Neither incident authorizes or supports a scientific claim.
