# Clean Rebuild Next Actions

## CLEAN3R2 Terminal Scope

- Active stage: `CLEAN3R2_MATH_REPAIR_COUNTER_CONTRACT_ROUTING_REPAIR_AND_S3_RESUME`.
- Code freeze: `8dd620ea6d9645b946a503275ddbf04d66c3baa2`.
- Runner freeze: `5209b6afeeb2b2a0ad99e640ffe50f8dd3b0c909`.
- Authorization document SHA-256: `eb267045d32af7ae407129b8b53fb6f4c8ed8a6dd75b7889a5a0fb53c3223237`.
- Terminal: `FAILED_TECHNICAL_SOLVER_MANIFEST_CONTRACT`.
- Formal S3 byte parity: `NOT_EVALUATED`.
- The sole attempt is consumed; `retry_count=0`; `ready_for_S4=false`; `ready_for_paper_claims=false`.

The loader set the exact CLEAN3R2 S3 role, but `PortRuntime::runFromConfig` overwrote it with the generic formal four-method role. The runner correctly rejected the actual manifest. Unsealed NAV/STD hashes equal the anchors, but the formal ledger, post-validation, seal, and stream comparison did not occur, so those files are not parity evidence.

Do not retry, start S4, generate calibration or module-effect artifacts, run the evaluator, open the reference trace, regenerate providers, perform the 3641/5951 rebind, or execute Canonical-541. A future provenance-routing repair is proposal-only until a human approves a new non-overwriting attempt identity and freeze/authorization chain. Manifest validation must not be weakened. The CLEAN1 material below is retained protocol history and is not an active execution authorization.

## CLEAN0 Closure

- Raw hash lock and post-delete full verification: complete.
- Independent paper-rebuild code/config/tests and active-core build: complete.
- BY2 `basic_dual_yaw_EKF` smoke from freshly regenerated raw-derived inputs: complete.
- `old_runtime_input_count=0`, clean code/provider provenance, and forbidden flags: verified.
- Exact guarded legacy deletion and storage accounting: complete.
- Clean GPT context export and lightweight unmerged review PR: CLEAN0 publication deliverables.

## CLEAN1R2R1 Authorized Scope

The human selected `CLEAN_REAL_DATA_FINAL_V23` and authorized the following bounded CLEAN1R2R1 work on the existing CLEAN1 branch and worktree:

1. Reuse the completed CLEAN1R2 archive/tag/static-contract recovery; do not repeat the 18 GB recursive inventory or broad extraction.
2. Classify E001 as semisynthetic diagnostic reference only. Retain its unaffected static runtime fields only where exact tag/source cross-confirms them; never use its old input or output as solver input or current evidence.
3. Verify current BY2 raw 22/22 and generate a fresh clean 15-column input from GNSS1 position/std, GNSS1 raw NAV-PVT velocity, GNSS1/GNSS2 status A1 yaw, and Go2 body IMU. Fix yaw measurement std at 1.5 degrees, disable yaw noise injection, and exclude trace from generation.
4. Fresh-build exact `final-v23-freeze`, run the clean exact-source anchor, and repair the active port only until exact-vs-active numerical parity closes without tuning.
5. Only after both parity gates pass, complete the code freeze and freshly regenerate the common provider at that commit.
6. Run the four methods exactly once and in order under the same recovered time window, initialization, preprocessing, measurement mapping, velocity policy, lever arm, executable/provider base, and evaluator: `single_antenna_EKF`, `basic_dual_yaw_EKF`, `strong_dual_yaw_EKF`, `LegSA_Paper_V1`.
7. Seal all four outputs before offline trace evaluation, cross-check the aggregates, preserve the superseded CLEAN1R1C evidence, and produce only the single terminal package authorized for PASS or a concrete BLOCKED outcome.

The recovered static contract fixes start/end, initialization, velocity/std behavior, yaw measurement semantics, scheme-C routing, writer, and evaluator identity. Historical metrics cannot select parameters or tolerance. The four-method chain is forbidden before the exact-source clean run and active-port parity gates pass.

The CLEAN1R2 terminal report remains a historical record of the wrong parity-anchor selection. It launched no solver, produced no formal run, read no current trace, and used no legacy solver input, so current active evidence was not contaminated. The human clean-real decision resolves that blocker; strict byte parity to E001 is no longer a CLEAN gate.

CLEAN1R2R1 keeps `paper_figure_count=0` and does not authorize classic-18, D01-D60, the 60x9 matrix, DA03, DA05, BY3, XB, PG, selected FGO feedback, active/complete nine-factor FGO, QM, QA, contact/FK factors, parameter search, per-case tuning, output-only correction, metric-driven epoch deletion, merge, release tag, or any next stage.
