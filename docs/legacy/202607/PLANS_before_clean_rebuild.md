# Pre-CLEAN0 Plan And Route Summary

This document freezes the prior roadmap at a high level. It is historical context only, not authorization and not performance evidence.

## Earlier Development Route

1. Build source-role, frame, manifest, evaluator, and repository-governance foundations.
2. Establish a source-backed EKF/port-core path and real-data diagnostic replay.
3. Add Raw Doppler, Go2 weak priors/diagnostics, source-aware weighting, no-feedback FGO candidates, and selected-feedback experiments.
4. Execute and review controlled BY2 degradation and internal-ablation work through guarded stage-specific runners.
5. Audit active-nine-factor FGO claims, then keep the nine-factor path at design/backend-blocked status rather than relabeling EKF evidence.
6. Extend review to BY3 position/up stress with yaw cautions, severe-GNSS XB/PG source boundaries, and design-only quality-aware fallback work.
7. Organize paper-facing evidence, baseline definitions, horizontal-method diagnostics, Go2/source-aware/QM boundaries, and storage-maintenance stages.

## Frozen Unfinished Or Bounded Routes

- Active nine-factor FGO implementation and representative validation were not established as an active paper claim.
- BY3 yaw remained bounded/diagnostic rather than ordinary yaw generalization.
- Severe-GNSS quality-aware fallback remained a separate design route.
- Exact external-method reproduction and comprehensive superiority were not established.
- Source-aware, QM, Raw Doppler, and Go2 interpretations required family-specific caveats.

## CLEAN0 Supersession

All earlier execution plans and readiness flags are superseded for active paper work. They remain recoverable in Git history and the verified bundle. The clean route begins from immutable raw data, fresh providers, the paper-rebuild runner, and new manifests under `<CLEAN_ROOT>`.
