# N8K2 Decision

The passing N8K2 status is `BY2_formal_ablation_real_plot_fix_complete`.

The recommended next stage after a passing fix is `N8K_merge_review_then_N9A_BY2_full_plot_audit`.

This remains an engineering plot audit. It does not change algorithms, does not run the full degradation matrix, and does not make paper performance claims.
