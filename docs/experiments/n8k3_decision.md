# N8K3 Decision

The passing N8K3 decision is `BY2_formal_ablation_duplicate_plot_fix_complete`.

The recommended next stage after this fix is `N8K_merge_review_then_N9A_BY2_full_plot_audit`.

This decision is only about duplicate plot semantics. It is not an N9A start signal, not a degradation matrix run, and not a paper performance claim.
