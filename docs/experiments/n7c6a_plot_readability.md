# N7C6A Plot Readability

N7C6A shortens long variant labels in runtime replacement figures, for example `joint_rp1p6deg_hv1p0` becomes `rp1.6/hv1.0`.

Replacement figures are runtime-only and are not committed. They clarify labels, y-axis units, legends, and metric namespace without changing algorithm data.
