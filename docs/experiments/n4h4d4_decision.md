# N4H4D4 Decision

N4H4D4 classifies the remaining clean replay failure using external trace parity, runtime loop parity, shadow measurement residuals, gain/feedback diagnostics, and earlier D1-D3 context.

Possible next stages include mechanization step parity, covariance/gain/feedback fix, update timeline fix, measurement model fix, or extended runtime tracing.

The decision report is diagnostic only. It does not hide failed parity and does not promote any shadow run as a result.
