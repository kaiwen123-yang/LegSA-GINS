# N7C6A Metric Semantics

N7C6A requires every figure to declare whether it shows raw series, variant delta, residual proxy, NIS proxy, cross-source consistency, or evaluation-only error.

Raw attitude or velocity series must not be named as truth error. Delta plots must explicitly state their comparator. No figure supports a paper performance claim.
