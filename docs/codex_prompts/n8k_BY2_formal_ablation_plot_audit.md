# N8K BY2 Formal Ablation Plot Audit Prompt

Task: run the BY2 paper-required formal ablation protocol and ablation plot
audit after N8J final validation.

Required actions:

- merge PR #47 and tag N8J first;
- create the N8K branch from latest main;
- lock the N8J selected feedback joint filter;
- run/report formal ablation variants;
- generate all required ablation plot categories under the BY2 plot audit role;
- generate case reviews, summary reports, and an N9B degradation plan;
- audit no algorithm changes, no full degradation matrix run, no trace/final_v23
  tuning, and no paper performance claim.

Do not merge the N8K PR or create an N8K tag.
