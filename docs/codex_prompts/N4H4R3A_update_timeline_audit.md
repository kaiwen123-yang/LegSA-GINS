# N4H4R3A Update Timeline Audit Prompt

Audit the source-backed port clean replay update timeline, effective overlap,
runtime config, and update-count decision. If evidence shows GNSS observations
are overwritten before update, apply only a source-backed runtime-loop fix.

Do not merge PR #25, tag R3A, merge PR #21, tune gates, delete epochs, perform
output-only correction, use final_v23 output as solver input, add factors, or
make performance claims.
