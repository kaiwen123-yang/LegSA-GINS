# N0 Bootstrap Prompt Summary

Bootstrap the clean `LegSA-GINS` repository as a new paper-oriented project for Legged State-Augmented Source-Aware GNSS/INS Smoothing.

Create only repository structure, governance documents, Git rules, phase-management files, placeholder tests, and audit scripts.

Do not import LegTC-FGO code, old step-chain logic, old result naming, old A0/C8 scripts, or old frozen solver logic.

Treat final_v23 only as a mature backbone reference, strong baseline, and evaluator sanity oracle. It is not the proposed method.

Do not implement algorithms in N0.
