# Context Reading Report

PAPER4B_R2 read the core repository context files and the available PAPER4A/PAPER3F/PAPER3G/PAPER3H/PAPER3I stage outputs. The stage starts from PAPER4A's conservative boundary: body-yaw RMSE and yaw superiority were still forbidden until physical antenna order and method yaw semantics could be closed.

Key finding for this stage: the new user physical evidence is sufficient to close the antenna-to-body geometry policy for GNSS1-right/GNSS2-left lateral installation, but each method still needs a separate native-yaw semantics check before body-yaw metrics can be promoted.

See `context_input_file_index.csv` and `missing_input_stage_paths.csv` for proof of found/missing inputs.
