schema_version: paper_rebuild.paths.v1
paths:
  code_root: "<CODE_ROOT>"
  raw_root: "<RAW_ROOT>"
  by2_fix_root: "<BY2_FIX_ROOT>"
  by2_go2_body: "<BY2_GO2_BODY>"
  by2h_fix_root: "<BY2H_FIX_ROOT>"
  by2h_go2_body: "<BY2H_GO2_BODY>"
  by2o_fix_root: "<BY2O_FIX_ROOT>"
  by2o_go2_body: "<BY2O_GO2_BODY>"
  clean_root: "<CLEAN_ROOT>"
  provider_root: "<CLEAN_ROOT>/04_PROVIDER_FREEZE/BY2_CLEAN"
  runtime_root: "<CLEAN_ROOT>/05_BY2_CLEAN"

# Copy this file to DATA_PATHS.local.yaml and replace placeholders locally.
# DATA_PATHS.local.yaml is ignored and must never be committed or exported.
