# PAPER10X_R2 Export Index

See `<PAPER10X_R2_C_EXPORT_ROOT>` for branch, PR, merge, tag, bundle, restore, and new-drive Git storage reports. The bundle binary itself is stored in the archive-center bundle directory, not in C export.
