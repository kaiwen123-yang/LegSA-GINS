# Contributing

Use phase branches and conventional commits.

Do not push directly to `main`.

Do not commit raw data, large logs, or generated full-result dumps.

Keep `final_v23` language bounded to backbone reference, strong baseline, and evaluator sanity oracle.

N0 is structure and governance only. Do not add solver implementation in this phase.
